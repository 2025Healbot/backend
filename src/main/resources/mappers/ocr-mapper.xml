<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.boot.domain.ocr.model.mapper.OCRMapper">

    <resultMap id="HospitalOcrInfoMap" type="com.hospital.boot.app.ocr.dto.HospitalOcrInfo">
        <id     property="hospitalId"   column="HOSPITAL_ID"/>
        <result property="hospitalName" column="HOSPITAL_NAME"/>
        <result property="address"      column="ADDRESS"/>
    </resultMap>

    <select id="findHospitalForOCR" resultMap="HospitalOcrInfoMap">
        SELECT HOSPITAL_ID, HOSPITAL_NAME, ADDRESS
        FROM   HOSPITALS
        WHERE  HOSPITAL_ID = #{hospitalId}
    </select>

</mapper>
